module GeneticProgramming.ShortDataModel

module ShortList =
  open Operators

  let empty pool = notImplemented()
  let cons pool head tail = notImplemented()
  let switch pool list onEmpty onPopulated = notImplemented()
  let length pool list = notImplemented()


